<!-- data/cron_job.xml -->
<odoo>
    <data >

       <record id="ir_cron_my_cron_daily_attendance" model="ir.cron">
            <field name="name">Create Daily Attendance</field>
            <field name="model_id" ref="ala_education_attendances.model_ala_education_attendance"/>
            <field name="state">code</field>
            <field name="code">model.generate_daily_attendance()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
       </record>

    </data>
</odoo>
