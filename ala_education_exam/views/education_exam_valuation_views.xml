<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!--   Form view for education exam valuation   -->
    <record id="education_exam_valuation_view_form" model="ir.ui.view">
        <field name="name">ala.education.exam.valuation.view.form</field>
        <field name="model">ala.education.exam.valuation</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_valuation_completed" type="object"
                            invisible="not mark_sheet_created or state != 'draft'"
                            class="oe_highlight" string="Completed"/>
<!--                    <button name="action_valuation_canceled" type="object" groups="ala_education_core.group_education_principal"-->
<!--                            invisible="state != 'draft'" string="Cancel"/>-->
                    <button name="action_set_to_draft" type="object"
                            invisible="state != 'completed'"
                            string="Set to Draft"/>
                    <field name="state" readonly="1" widget="statusbar"
                           statusbar_visible="draft,completed"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_open_exam_valuation_lines" type="object"
                                widget="statinfo"
                                groups="ala_education_core.group_education_principal"
                                class="oe_stat_button" icon="fa-calendar"
                                string="Marks"/>
                    </div>
                    <h1>
                        <field name="name" readonly="1"/>
                    </h1>
                    <group>
                        <group>
                            <field name="exam_id" options="{'no_create': True, 'no_open':True}"
                                   readonly="1"/>
                            <field name="division_id" options="{'no_create': True, 'no_open':True}" domain="[('id', 'in', division_ids)]"
                                   readonly="1"/>
                            <field name="subject_ids" invisible="1" />
<!--                            <field name="edu_faculty_ids" invisible="1" />-->
                            <field name="division_ids" invisible="1" />
                            <field name="subject_id" domain="[('id', 'in', subject_ids)]"
                                   readonly="1" options="{'no_create': True, 'no_open':True}"/>
                            <field name="faculty_ids"
                                   readonly="1"  options="{'no_create': True, 'no_open':True}" invisible="1"/>
<!--                            <field name="user_id"  options="{'no_create': True, 'no_open':True}"/>-->
<!--                            <field name="assign_marks"  widget="voice_to_marks" readonly="state != 'draft'" invisible="is_assign_mark == False"/>-->
                        </group>
                        <group>
                            <field name="mark" readonly="1" force_save="1"/>
                            <field name="pass_mark" force_save="1"
                                   readonly="1"/>
                            <field name="date" readonly="1"/>
                            <field name="class_id"
                                   readonly="state != 'draft' or mark_sheet_created" invisible="1"/>
                            <field name="division_id"
                                   readonly="state != 'draft' or mark_sheet_created " invisible="1"/>
                            <field name="academic_year_id" invisible="1"/>
                            <field name="mark_sheet_created" invisible="1"/>
                            <field name="is_assign_mark" invisible="1"/>
<!--                            <field name="exam_marks"  widget="voice_to_marks" readonly="state != 'draft'"/>-->
                        </group>
                    </group>
                    <div style="padding-bottom: 10px;">
                        <button style="padding: 1px 5px;"
                                invisible="state == 'cancel' or mark_sheet_created "
                                string="Create Mark Sheet"
                                name="action_create_mark_sheet" type="object"
                                class="oe_highlight"/>
                    </div>
                    <notebook invisible="not mark_sheet_created ">
                        <page string="Mark Sheet">
                            <field name="valuation_line_ids"
                                   readonly="state != 'draft'">
                                <list create="0" editable="bottom">
                                    <field name="student_id"
                                           column_invisible="1"/>
                                    <field name="student_name" readonly="1"/>
                                    <field name="roll_no"  readonly="1"/>
                                    <field name="exam_mark" />
                                    <field name="assign_mark" column_invisible="not parent.is_assign_mark"/>
                                    <field name="mark_scored" decoration-danger="pass_or_fail != True" decoration-success="pass_or_fail == True" readonly="1" force_save="1"/>
                                    <field name="pass_or_fail" column_invisible="1"
                                           />
                                    <field name="valuation_id"
                                           column_invisible="1"/>
                                </list>
                                <form>
                                    <sheet>
                                        <group>
                                            <field name="student_id"/>
                                            <field name="mark_scored"/>
                                            <field name="pass_or_fail"
                                                   widget="boolean_toggle"/>
                                            <field name="valuation_id"
                                                   invisible="1"/>
                                        </group>
                                    </sheet>
                                </form>
                            </field>
                        </page>
                    </notebook>
                    <button name="action_valuation_completed" type="object"
                            invisible="not mark_sheet_created or state != 'draft'"
                            class="oe_highlight" string="Completed"/>
                </sheet>
                <chatter
                    open_attachments="True"
                    reload_on_attachment="True"
                    reload_on_follower="True"
                    reload_on_post="True"
                />
            </form>
        </field>
    </record>
    <!--   Tree view for education exam valuation   -->
    <record id="education_exam_valuation_view_tree" model="ir.ui.view">
        <field name="name">ala.education.exam.valuation.view.tree</field>
        <field name="model">ala.education.exam.valuation</field>
        <field name="arch" type="xml">
            <list>
                <field name="exam_id"/>
                <field name="subject_id"/>
                <field name="class_id"/>
                <field name="division_id"/>
                <field name="faculty_ids"  widget="many2many_tags"/>
                <field name="state" widget="badge" decoration-success="state == 'completed'"
                                                                            decoration-info="state == 'draft'" decoration-muted="state == 'cancel'"/>
                <field name="academic_year_id"/>
            </list>
        </field>
    </record>


<!--    Exam Valuation line tree-->
    <!--   Tree view for education exam valuation   -->
    <record id="exam_valuation_line_view_tree" model="ir.ui.view">
        <field name="name">ala.exam.valuation.line.view.tree</field>
        <field name="model">ala.exam.valuation.line</field>
        <field name="arch" type="xml">
            <list>
                <field name="valuation_id"/>
                <field name="student_id"/>
                <field name="roll_no"/>
                <field name="exam_mark"/>
                <field name="assign_mark"/>
                <field name="mark_scored"/>
                <field name="pass_or_fail"/>
            </list>
        </field>
    </record>

    <record id="view_education_exam_valuation_search" model="ir.ui.view">
    <field name="name">ala.education.exam.valuation.search</field>
    <field name="model">ala.education.exam.valuation</field>
    <field name="arch" type="xml">
        <search>
            <field name="exam_id"/>
            <field name="academic_year_id"/>
            <field name="class_id"/>
            <field name="subject_id"/>
            <field name="faculty_ids"/>
            <field name="state"/>

            <filter string="Current Academic Year"
                    name="current_academic_year"
                    domain="[('academic_year_id.enable', '=', True)]"/>

            <separator/>

            <filter string="Draft"
                    name="draft"
                    domain="[('state', '=', 'draft')]"/>

            <filter string="Completed"
                    name="completed"
                    domain="[('state', '=', 'completed')]"/>

            <group>
                <filter string="Class"
                        name="group_class"
                        context="{'group_by': 'class_id'}"/>

                <filter string="Exam"
                        name="group_exam"
                        context="{'group_by': 'exam_id'}"/>

                <filter string="Subject"
                        name="group_subject"
                        context="{'group_by': 'subject_id'}"/>

                <filter string="Status"
                        name="group_state"
                        context="{'group_by': 'state'}"/>
            </group>
        </search>
    </field>
</record>


    <!--   Action for education exam valuation   -->
    <record id="education_exam_valuation_action" model="ir.actions.act_window">
        <field name="name">Exam Marks Entry</field>
        <field name="res_model">ala.education.exam.valuation</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'create': False, 'search_default_current_academic_year': 1, 'search_default_group_exam': 1,
            'search_default_group_class': 1, 'search_default_group_subject': 1, 'search_default_group_state': 1 }</field>
    </record>
    <!--   Menu Items   -->
    <menuitem id="examination_valuation_menu"
              web_icon="ala_education_exam,static/description/mark_icon.png"
              action="education_exam_valuation_action" name="Exam Marks Entry"
              sequence="18"/>
</odoo>
